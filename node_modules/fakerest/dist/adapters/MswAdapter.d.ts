import { APIServer, NormalizedRequest } from '../types.ts';
import { BaseServerOptions } from '../SimpleRestServer.ts';

export declare class MswAdapter {
    server: APIServer;
    constructor({ server, ...options }: MswAdapterOptions);
    getHandler(): ({ request }: {
        request: Request;
    }) => Promise<Response>;
    getNormalizedRequest(request: Request): Promise<NormalizedRequest>;
}
export declare const getMswHandler: (options: MswAdapterOptions) => ({ request }: {
    request: Request;
}) => Promise<Response>;
export type MswAdapterOptions = BaseServerOptions & {
    server?: APIServer;
};
