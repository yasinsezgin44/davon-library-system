package com.davon.library.model.enums;

/**
 * Enum representing the status of a user account
 * Maps to the user_status enum in the database
 */
public enum UserStatus {
    ACTIVE,
    INACTIVE,
    SUSPENDED
} 