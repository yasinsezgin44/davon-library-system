package com.davon.library.model.enums;

/**
 * Enum representing the status of a fine
 * Maps to the fine_status enum in the database
 */
public enum FineStatus {
    PENDING,
    PAID,
    WAIVED,
    DISPUTED
} 