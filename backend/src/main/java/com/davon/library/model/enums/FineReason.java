package com.davon.library.model.enums;

/**
 * Enum representing the reason for a fine
 * Maps to the fine_reason enum in the database
 */
public enum FineReason {
    OVERDUE,
    DAMAGED_ITEM,
    LOST_ITEM,
    ADMINISTRATIVE
} 