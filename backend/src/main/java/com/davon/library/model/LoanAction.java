package com.davon.library.model;

/**
 * Enum representing the different actions that can be performed on a loan.
 */
public enum LoanAction {
    CHECKOUT,
    RETURN,
    RENEWAL,
    OVERDUE_NOTICE
}